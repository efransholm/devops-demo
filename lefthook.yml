# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
pre-push:
  jobs:
    - name: linting
      run: |
        eslint . && echo "✅ Linting passed!" || (echo "❌ Linting failed! Push blocked." && exit 1)
#
#     - name: gems audit
#       tags:
#         - backend
#         - security
#       run: bundle audit
#
pre-commit:
  parallel: false
  jobs:
    - name: testing
      run: |
        npm test && echo "✅ All tests passed!" || (echo "❌ Tests failed! Commit blocked." && exit 1)

    # - name: rubocop
    #   glob: "*.rb"
    #   exclude:
    #     - config/application.rb
    #     - config/routes.rb
    #   run: bundle exec rubocop --force-exclusion {all_files}

#     - name: govet
#       files: git ls-files -m
#       glob: "*.go"
#       run: go vet {files}
#
#     - script: "hello.js"
#       runner: node
#
#     - script: "hello.go"
#       runner: go run
